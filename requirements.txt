## Top-level requirements (unpinned) — installs latest stable releases
# Use Poetry to lock exact versions into poetry.lock for reproducible installs.

rich-click
rich
python-frontmatter
PyYAML
gql[all]
pydantic

# Dev / tooling
python-semantic-release
flake8
black
pre-commit

requests
